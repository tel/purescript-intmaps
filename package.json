{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "pulp test && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "pulp": "^7.0.0",
    "purescript": "^0.7.6",
    "rimraf": "^2.5.0"
  }
}
